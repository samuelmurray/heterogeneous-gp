os:
  - linux
language: python
dist: xenial
python:
  - "3.6"
  - "3.7"
install:
  - pip install pipenv
  - pipenv install --dev --ignore-pipfile --system
  - pipenv install .[tf] --skip-lock
script:
  - pipenv run pytest --testexamples